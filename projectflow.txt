Building Pipeline:
1>Create a Github repe and clone it in local (Add experiments).
2>Add src folder with all components(run them individuallly).
3>Add data,models,reports directories to .gitignore file
4>Now git add,commit ,push